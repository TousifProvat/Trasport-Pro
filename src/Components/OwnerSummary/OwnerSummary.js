import React from 'react';
import useContext from '../Hooks/useContext'

const OwnerSummary = () => {
    // const { terminalData } = useContext();
    // console.log(terminalData);
    return (
        <div>
            <h1>I am from owner summary.</h1>
        </div>
    );
};

export default OwnerSummary;
